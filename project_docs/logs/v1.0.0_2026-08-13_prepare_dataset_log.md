---
version: "1.0.0"
date: "2026-08-13"
type: "log"
status: "COMPLETED"
author: "AI Agent"
target_component: "Data Preprocessing"
tags: ["log", "setup", "dataset"]
summary: "Nhật ký khởi tạo script prepare_dataset.py và tạo các template tài liệu kế hoạch cho quá trình trích xuất Joern."
---

# Nhật ký Cập nhật: Khởi tạo Script Dataset & Tài liệu (Phiên bản v1.0.0)

## 1. Mục Đích Công Việc (Purpose)
- Triển khai kịch bản tải bộ dữ liệu Devign và Reveal từ HuggingFace và ghi thành các file `.c` riêng lẻ, hỗ trợ trực tiếp việc chạy Joern parse ra đồ thị CPG.
- Đồng bộ hóa tài liệu dự án trong mục `project_docs` theo chuẩn đã định ra cho con người và AI Agent.

## 2. Thay Đổi Kỹ Thuật (Technical Changes)
- **Code**:
  - Tạo mới file `prepare_dataset.py` tại root.
  - Sử dụng thư viện `datasets` của HuggingFace, kết hợp hàm `tqdm` để theo dõi tiến độ.
  - Phân tách theo cấu trúc: `data/raw_c_files/<dataset_name>/<split>/id_<id>.c`.
  - Sinh kèm một file metadata dạng JSON để lưu vết các cột thông tin từ HF sang ID file vật lý (nhằm gộp lại sau này).
  
- **Tài liệu**:
  - `plans/v1.0.0_2026-08-13_joern_graph_extraction_plan.md`: Đã lên plan cụ thể 5 bước triển khai cho Dual Boot.
  - `progress/v1.0.0_2026-08-13_joern_graph_extraction_progress.md`: Cập nhật checklist, phase 1 hoàn tất khoảng 20%.

## 3. Quá Trình Kiểm Thử (Testing & Results)
- Đoạn code `prepare_dataset.py` đã chuẩn bị sẵn sàng, nhưng hiện tại cần môi trường ảo trên Windows cài đặt `datasets` và `pandas` để tiến hành thực thi và sinh data.

## 4. Các Lưu Ý Bổ Sung (Additional Notes / Context)
- Cần chạy lệnh cài đặt pip: `pip install datasets pandas` trên môi trường Windows.
- Khi chạy Joern, chỉ nên chạy thử 10-20 files để đối chiếu cấu trúc xuất đồ thị trước khi chạy đồng loạt toàn bộ hàng chục ngàn tệp tin mã nguồn.
