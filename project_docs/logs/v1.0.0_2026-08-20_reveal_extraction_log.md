---
version: "1.0.0"
date: "2026-08-20"
type: "log"
status: "COMPLETED"
author: "AI Agent"
target_component: "Data Preprocessing"
tags: ["log", "reveal", "dataset", "joern", "openjdk"]
summary: "Hoàn tất trích xuất 22.734 file .c cho Reveal từ claudios/ReVeal và xác nhận OpenJDK 11 trên Ubuntu."
---

# Nhật Ký Thực Hiện: Trích Xuất Dữ Liệu Reveal & Kiểm Tra Java

## 1. Mục Đích Công Việc
- Kiểm tra nguồn dataset Reveal uy tín trên Hugging Face.
- Nâng cấp `prepare_dataset.py` để tải dữ liệu chất lượng cao từ `claudios/ReVeal` (kèm fallback).
- Trích xuất toàn bộ các hàm C/C++ thành các file `.c` riêng lẻ phục vụ quá trình Joern CPG extraction.
- Xác nhận Java 11 đã được cài đặt và kích hoạt thành công trên môi trường Ubuntu.

## 2. Kết Quả Chi Tiết
- **Nguồn dữ liệu sử dụng**: `claudios/ReVeal` (chuẩn benchmark gốc với 22.734 mẫu và đầy đủ metadata).
- **Số lượng file .c đã trích xuất thành công**:
  - `data/raw_c_files/reveal/train/`: **18.187** file `.c` (kèm `train_metadata.json`)
  - `data/raw_c_files/reveal/validation/`: **2.273** file `.c` (kèm `validation_metadata.json`)
  - `data/raw_c_files/reveal/test/`: **2.274** file `.c` (kèm `test_metadata.json`)
  - Tổng số file mã nguồn thô hiện có (Devign + Reveal): **50.052** file `.c`.
- **Môi trường Java**: `OpenJDK 11.0.31 (64-Bit Server VM)` hoạt động bình thường.

## 3. Bước Tiếp Theo
- Tiến hành chạy thử `joern-parse` trên 1-2 file mẫu để đối chiếu định dạng `nodes.csv` và `edges.csv`.
- Xây dựng bộ trích xuất hàng loạt đa tiến trình với timeout protection.
